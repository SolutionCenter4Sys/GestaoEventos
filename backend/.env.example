# Supabase Database (PostgreSQL)
# Se a senha tiver @, #, etc., codifique: @ -> %40, # -> %23, [ -> %5B, ] -> %5D
DATABASE_URL="postgresql://postgres:SUA_SENHA@db.vrliyffjqwqxnixwwhbk.supabase.co:5432/postgres"

# JWT
JWT_SECRET="CHANGE_THIS_TO_STRONG_SECRET_KEY_MIN_32_CHARS"
JWT_EXPIRES_IN="15m"
REFRESH_TOKEN_EXPIRES_IN="7d"

# Encryption (LGPD - Dados sensíveis de Paciente Modelo)
# Gerar com: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY="CHANGE_THIS_TO_BASE64_32_BYTES_KEY"

# Frontend URL
FRONTEND_URL="http://localhost:4200"

# Redis (BullMQ - Fila de E-mails)
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""

# reCAPTCHA Google
RECAPTCHA_SECRET_KEY="YOUR_RECAPTCHA_SECRET_KEY"

# Email Provider
EMAIL_PROVIDER="supabase" # ou "ses", "sendgrid", "smtp"
EMAIL_FROM="noreply@plataforma-eventos.com"

# AWS SES (se usar EMAIL_PROVIDER="ses")
AWS_REGION="us-east-1"
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""

# SendGrid (se usar EMAIL_PROVIDER="sendgrid")
SENDGRID_API_KEY=""

# SMTP (se usar EMAIL_PROVIDER="smtp")
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""

# Supabase (opcional - para Supabase Auth ou Storage)
SUPABASE_URL="https://vrliyffjqwqxnixwwhbk.supabase.co"
SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"
SUPABASE_SERVICE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"

# Node Environment
NODE_ENV="development" # ou "production"
PORT="3000"

# Rate Limiting
RATE_LIMIT_TTL="60"
RATE_LIMIT_MAX="10"

# Prometheus Metrics (opcional)
METRICS_ENABLED="true"
METRICS_PORT="9090"
